<%= form_with(model: bagmon, local: true) do |form| %>
  <% if bagmon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bagmon.errors.count, "error") %> prohibited this bagmon from being saved:</h2>

      <ul>
      <% bagmon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :force %>
    <%= form.number_field :force %>
  </div>

  <div class="field">
    <%= form.label :agility %>
    <%= form.number_field :agility %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon %>
  </div>


<div class="field">
    <%= form.label :category_id %>
    <%= form.select("category_id", @category_options, {include_blank: 'Selecione...'}, {:multiple => false}) %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
